# image-compression-app-in-nodejs

This app is created for blog article purpose.

make sure to run `yarn` before using this, also make a folder inside `images` folder named `temp` otherwise code will be breaked.
